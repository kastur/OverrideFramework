<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="android.override.providers">
    <uses-sdk android:minSdkVersion="15"/>
    <application android:icon="@drawable/icon" android:label="@string/app_name">
        <service android:name=".RandomWalkLocationProvider">
            <intent-filter>
                <action android:name="android.override.providers.RandomWalkLocationProvider"/>
            </intent-filter>
        </service>
    </application>
</manifest>
